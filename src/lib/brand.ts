/** LinkUp brand copy — splash, auth, marketing surfaces. */
export const APP_NAME = 'LinkUp';

/** Primary wordmark asset (1024×341). */
export const APP_LOGO_PATH = '/linkup-logo.png';
export const APP_LOGO_WIDTH = 1024;
export const APP_LOGO_HEIGHT = 341;

export const APP_TAGLINE = 'Discover meetups near you.';

export const APP_TAGLINE_SECONDARY = 'Meet with confidence.';

/** Splash lockup — wordmark + tagline (1024×364, transparent PNG). */
export const APP_SPLASH_LOCKUP_PATH = '/splash-brand-lockup.png';
export const APP_SPLASH_LOCKUP_WIDTH = 1024;
export const APP_SPLASH_LOCKUP_HEIGHT = 364;

export const APP_SPLASH_BACKGROUND = '#FBE9E9';

/** Minimum branded splash duration on cold start (ms). */
export const APP_SPLASH_DURATION_MS = 1200;
