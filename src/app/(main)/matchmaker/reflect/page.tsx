import { MatchMakerReflect } from '@/features/matchmaker/MatchMakerReflect';

export const metadata = { title: 'MatchMaker — Reflection' };

export default function MatchMakerReflectPage() {
  return <MatchMakerReflect />;
}
