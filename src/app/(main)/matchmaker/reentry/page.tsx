import { MatchMakerReEntry } from '@/features/matchmaker/MatchMakerReEntry';

export const metadata = { title: 'MatchMaker — Re-entry' };

export default function MatchMakerReEntryPage() {
  return <MatchMakerReEntry />;
}
